{
  "name": "astb-pbm",
  "private": true,
  "type": "module",
  "description": "Local ASTB-E PBM trainer: X-52 stick/throttle tracking + dichotic listening",
  "scripts": {
    "start": "./serve.sh",
    "test": "node --test tests/*.test.mjs",
    "audio": "python3 scripts/generate-audio.py",
    "check": "node --test tests/*.test.mjs && python3 scripts/generate-audio.py"
  }
}
